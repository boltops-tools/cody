version: 0.2

# Example starter file
# Edit to fit your needs

phases:
  install:
    commands:
      - uname -a
      - pwd
      - ls
      - env | sort
      - ls /etc/*release*
      - cat /etc/*release*
      - whoami
      # - bundle
  build:
    commands:
      - echo Build started on `date`
      - uptime
      # - bundle exec rspec
# cache:
#   paths:
#     - /usr/local/bundle
#     - /usr/local/lib/ruby/gems/2.5.0
# artifacts:
#   files:
#     - result.txt
